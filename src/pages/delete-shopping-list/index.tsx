import { type JSX } from 'preact';
/* import { useRoute } from 'preact-iso'; */

export function DeleteShoppingList(): JSX.Element {
  /*   const {
    params: { shoppingListId },
  } = useRoute(); */

  return <p>tu veux vraiment supprimer la liste ?</p>;
}
